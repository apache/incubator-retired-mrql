/* DBLP data from http://dblp.uni-trier.de/xml/ */

S = source(xml,"/home/fegaras/data/dblp.xml",{"article","incollection","book","inproceedings"});

select (x,count(a))
from a in S,
     c in a.cite
where text(c) <> "..."
group by x: text(c)
order by inv(count(a));
